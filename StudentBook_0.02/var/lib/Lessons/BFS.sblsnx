Breadth First Search

BFS(v)
    queue q
    q.push(v)
    while (!q.empty())
        used[v] = true
        for (children(v))
            if (!used[v] && !inq[v]))
                q.push(v)
                inq[v] = true;
        handling()
        q.pop()
