File Types
    sblsnx
        text files, represent lessons
    as
        array of string
    aai
        array of array of integer
    sbclm
        array of ChooseLessonMenu
    ai
    array of string

.sbclm reading
    int size
        boolean f //isLesson
        string name
        if (!f)
            int childrenSize
                int childId
        else
            lessonId

